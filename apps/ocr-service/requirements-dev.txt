# Everything the service needs, plus what its test suite needs. The Dockerfile
# installs requirements.txt only, so none of this reaches the runtime image.
-r requirements.txt

pytest==8.3.4
httpx==0.28.1
