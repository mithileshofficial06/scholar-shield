# OCR + forensics service (PROJECT_REPORT.md §5 Tier 2, milestone Week 4).
#
# Pinned because the published metrics are only reproducible against the
# versions that produced them. Tesseract itself is a system binary, not a
# wheel — see README.md.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
pytesseract==0.3.13
pillow==11.1.0
numpy==2.2.1

# Dev / test
pytest==8.3.4
httpx==0.28.1
